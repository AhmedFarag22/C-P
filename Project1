#include<iostream>
#include "Source.h"
using namespace std;
int t = 10;
int main() {
	/*
	  int x = 5000;
	float c = 1.3;
	double m = 1.88;
	char l = 'j';
	string o = "sdsd";
	short y =  70000;
	bool q = true;
	cout << "x=\t" <<x ;
	cout << "\nc=\t" << c;
	cout << "\nm=\t" << m;
	cout << "\nl=\t" << l;
	cout << "\no=\t" << o;
	cout << "\ny=\t" << y;
	cout << "\nq=\t" << q;
	//cin >> c =445 ;
	
		//return 0;
	cout << "\nahmed";
	
	 */
	int t = 5;
	switch (2) {
	case 1: cout << '1';
		break;
	default: cout << '3';
	}

	cout << (5 < 7) ? true : false;
	int x = 0 , n=0;
	//x*= 10;
	float y = 20.6;
	cin >> x;
	cout << x++ <<" jhjhjhjmh"<< x++ << endl;
//	cin>>"/n farag 1\n"
	//cout << static_cast<int>(y);
	int len;
	string l, u;
	cin >> u >> l;

	if (l.length() >= u.length())
		len = l.length();

	else
		len = u.length();
	for (int i = 0; i < len; i++) {
		l[i] = tolower(l[i]);// to make it small
		u[i] = tolower(u[i]);
	}
}//if char < 97 then char is capital
//if char > 97 then char is small

/*
for(int i = 0; i < s.length(); ++i)
	{
		if(islower(s[i]))
			++cnt1;
		else
			++cnt2;
		s1 += toupper(s[i]);
		s2 += tolower(s[i]);
	}
	*/



